# THE BUTTON

Adaptive Godot prototype. The button reacts to how the player behaves rather than following a fixed sequence.

Every push to `main` reconstructs the verified gameplay script, runs Godot parse/boot/self-tests, exports the Web build, and deploys it to GitHub Pages.
